===============================
SEP 2 — Nomenclature guidelines
===============================

:Authors: Klemen Zaletelj <klemen.zaletelj@fs.uni-lj.si>, Janko Slavič <janko.slavic@fs.uni-lj.si>
:Status: Draft
:Type: Process
:Created: 2021-03-15


Abstract
--------

The nomenclature guidelines for SDyPy are presented for better interoperability
of the packages/modules.

Motivation
----------

The nomenclature is often not consistent for different packages which affects the
user experience when combining the use of multiple packages.

Detailed description
--------------------

SDyPy nomenclature follows the python guidelines, presented in 
`PEP 8 <https://www.python.org/dev/peps/pep-0008/>`_.

General guidelines
^^^^^^^^^^^^^^^^^^

* As is stated in `PEP 8 <https://www.python.org/dev/peps/pep-0008/>`_, the code is read 
  much more often than it is written. The names of the variables should therefore
  be as descriptive as possible.
  Examples:

  .. code:: python
  
     a = 4 # Not good

     rectangle_height = 4 # Good

  With most code editors enabling the use of autocomplete, the length of the variables does not present
  a major issue for code writing.

* When combining multiple words into a variable name, the term with a broader meaning should
  be on the first place. The more defining term comes next. This improves the readability of the code,
  enabling the reader to immediately understand what the main purpose of the variable is.  Examples:

  .. code:: python

     freq_upper
     freq_lower
     damping_viscous
     damping_hysteretic

  It should be said, that the it is up to the user to determine what is the more broader term.
  This guideline is a **heuristic for coining new names, not a rule that overrides the
  canonical table below**. Where the table has an entry for a quantity, that entry is
  normative, and a name that conforms to it is never a word-order violation: ``natural_freq``
  and ``frf_form`` both place the broader term second and are canonical nonetheless. The
  heuristic applies only when a new compound name is coined for a quantity the table does
  not cover, and it yields to an established term of art — ``mass_matrix``, not
  ``matrix_mass``. A name settled that way is then added to the table, so the decision is
  recorded rather than re-litigated.

Common variables in structural dynamics
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

Despite the general guidelines written above, the variables which are broadly used in structural dynamics 
can be an exception. To avoid different variable names (e.g. sampling_frequency, 
freq_sampling), the conventions given below should be used.

.. canonical-table-start

The **Symbol** column gives the canonical name. The **Instead of** column gives
the divergent spellings that name replaces, and is normative: this table is the
single source of truth for the nomenclature migration map, and no checker,
roster or other document may require a rename this column does not record. A
canonical name that replaces no evidenced divergent spelling leaves the cell
empty rather than inventing one. Adding a spelling to the column is an amendment
to this SEP and follows the same review path as any other amendment to it.

.. list-table::
   :header-rows: 1
   :widths: 18 20 20 7 35

   * - Parameter
     - Symbol
     - Instead of
     - Unit
     - Description
   * - Frequency limits
     - ``freq_upper``, ``freq_lower``
     - ``lower``, ``upper``, ``f_lower``, ``f_upper``
     -
     -
   * - Frequency Response Function
     - ``frf``
     -
     -
     - Different forms of the frequency response function are possible (receptance, mobility, accelerance) and can be given other names. When the frequency response function is referenced in arbitrary form, this variable name should be given.
   * - Frequency vector
     - ``freq``
     - ``frequency``
     - Hz
     -
   * - Frequency vector in rad/s
     - ``freq_rad``
     -
     - rad/s
     -
   * - Natural frequency
     - ``natural_freq``
     - ``nat_freq``
     - Hz
     -
   * - Sampling frequency
     - ``fs``
     - ``sampling_frequency``, ``freq_sampling``
     - Hz
     - This name is not descriptive; however, it is commonly used in other packages (e.g. `scipy <https://www.scipy.org/scipylib/index.html>`_)
   * - Time step
     - ``dt``
     -
     - second
     -
   * - FRF form
     - ``frf_form``
     - ``frf_type``
     -
     - Form of the frequency response function: ``'receptance'``, ``'mobility'`` or ``'accelerance'``. Canonical name at the sdypy level; backend parameter names are out of scope.
   * - Young's modulus
     - ``young_modulus``
     - ``E``, ``Young``
     - Pa
     - Fixed compound term; the broader-term-first rule does not reorder it.
   * - Poisson's ratio
     - ``poisson_ratio``
     - ``nu``, ``Poisson``
     -
     - Fixed compound term.
   * - Density
     - ``density``
     - ``rho``, ``ro``, ``Density``
     - kg/m^3
     -
   * - Mode shape
     - ``mode_shape``
     - ``phi``, ``phi_X``, ``phi_A``
     -
     - Mode shape vector or matrix. Exception: the criterion functions ``MAC``, ``MSF`` and ``MCF`` keep the modal-literature symbols ``phi_X`` (experimental) and ``phi_A`` (analytical) for their arguments; ``phi`` elsewhere is not canonical.
   * - Damping ratio
     - ``damping_ratio``
     - ``nat_xi``, ``pole_xi``
     -
     - Viscous damping ratio. Follows the ``damping_*`` word order of the general guidelines.
   * - Number of modes
     - ``n_modes``
     -
     -
     - Count of modes. Instance of the ``n_<plural>`` count convention (see below).
   * - Mass matrix
     - ``mass_matrix``
     - ``M``
     - kg
     - Global mass matrix. Replaces the single-letter ``M`` in public signatures; local variables inside a numerical routine are unaffected.
   * - Stiffness matrix
     - ``stiffness_matrix``
     - ``K``
     - N/m
     - Global stiffness matrix. Replaces ``K`` in public signatures. ``EI`` is **not** a divergent spelling of this name -- see the note below the table.
   * - Damping matrix
     - ``damping_matrix``
     - ``C``
     - Ns/m
     - Global viscous damping matrix. Replaces ``C`` in public signatures.
   * - Node coordinates
     - ``nodes``
     - ``org``
     -
     - Node coordinate array of a finite-element mesh.
   * - Element connectivity
     - ``elements``
     - ``conec``
     -
     - Element connectivity array of a finite-element mesh.
   * - Element natural coordinates
     - ``xi``, ``eta``, ``zeta``
     -
     -
     - Natural (isoparametric) coordinates of a finite element. An established term of art in finite-element analysis, and canonical as written: these are **not** divergent spellings of ``damping_ratio``.

Two entries need a word of explanation.

``EI`` is a scalar bending rigidity, the product of Young's modulus and the
second moment of area. It is not a stiffness matrix, so ``stiffness_matrix`` is
the wrong name for it and this table records no canonical spelling for it. It
remains a violation of the snake_case rule for public parameters, and the
package that exposes it replaces it with a descriptive snake_case name of the
maintainer's choosing.

``xi`` is canonical for an element natural coordinate and divergent for a
damping ratio, so its reading depends on context. A name that exposes a damping
ratio uses ``damping_ratio`` wherever it appears; a finite-element routine keeps
``xi``, ``eta`` and ``zeta``. Only the unambiguous damping spellings
(``nat_xi``, ``pole_xi``) are listed in the **Instead of** column, because a
bare ``xi`` cannot be classified without reading what the routine computes.

.. canonical-table-end

Naming of public objects
^^^^^^^^^^^^^^^^^^^^^^^^

The guidelines above cover variables. For all other public names in SDyPy
first-level packages the following conventions apply (PEP 8, made explicit):

* **Modules and packages**: ``snake_case`` words. Established acronym
  sub-package names fixed by SEP 3 / SEP 4 (``EMA``, ``FRF``, ``OMA``) stay
  uppercase.
* **Classes**: ``CapWords``; acronyms keep their uppercase form inside
  ``CapWords`` names (``FRF``, not ``Frf``).
* **Functions, methods and parameters**: ``snake_case``; acronyms are written
  in lowercase inside ``snake_case`` names (``add_frf``, ``frf_form``).
  Exception: established criterion functions may be the bare uppercase
  acronym (``MAC``, ``MSF``, ``MCF``), and those three functions also keep the
  modal-literature symbols ``phi_X`` (experimental mode shape), ``phi_A``
  (analytical mode shape) and ``phi`` as their arguments. This exception covers
  those three names only: ``phi`` as a parameter of any other public function is a
  violation of the canonical ``mode_shape``.
* **Counts**: a public name denoting a number of items is spelled ``n_<plural>``
  (``n_modes``, ``n_nodes``, ``n_elements``, ``n_frames``). A bare ``n`` is not a
  count in a public signature.
* **Indices**: a public name denoting an index into a collection is spelled
  ``<name>_idx`` (``node_idx``, ``elem_idx``). The ``_ind`` suffix is not used.
* **Constants**: ``ALL_CAPS``.
* **Framework exemption**: names mandated by an external framework keep that
  framework's casing (e.g. the Qt ``closeEvent`` override).

Public API surface
^^^^^^^^^^^^^^^^^^

Every first-level package declares its public API with an explicit
``__all__`` in its ``sdypy/<name>/__init__.py``. Only the names curated in
``__all__`` are supported API; any other module attribute is an
implementation detail. Packages that wrap a backend re-export the curated
names explicitly instead of star-importing the backend, so third-party and
standard-library names never enter the public surface.

Deprecation policy
^^^^^^^^^^^^^^^^^^

When a public name is renamed to conform to these guidelines, the old name is
kept as an alias that emits ``DeprecationWarning`` for all of v1.x. Removal of
a deprecated alias happens no earlier than v2.0. New code uses the canonical
names only.

Discussion
----------

This section may just be a bullet list including links to any discussions
regarding the SEP:

- This includes links to mailing list threads or relevant GitHub issues.


References and Footnotes
------------------------

.. [1] Each SEP must either be explicitly labeled as placed in the public domain (see
   this SEP as an example) or licensed under the `Open Publication License`_.

.. _Open Publication License: https://www.opencontent.org/openpub/


Copyright
---------

This document has been placed in the public domain. [1]_
